const SERVER = require("../../../Schemas/server/server"); 

function findUser(req, res, next) {
    
    try {
       
        
    } catch (message) {
        console.log(message)
    }
    
}

module.exports = findUser